{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "what the settings window may reach: core defaults only",
  "windows": [
    "settings"
  ],
  "permissions": [
    "core:default"
  ]
}
