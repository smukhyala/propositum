node_modules/
.next/
out/
dist/
build/

# Secrets — never commit
.env
.env.local
.env.*.local
# the private half of the extension's pinned key (extension/README.md, Before a real install)
*.pem

# Local database
*.db
*.db-journal
*.sqlite
*.sqlite3
prisma/*.db*

# Generated by Next — differs between `next dev` and `next build`, so it churns
next-env.d.ts

# Test / build output
coverage/
*.tsbuildinfo
.vitest/

# OS
.DS_Store

# Agent worktrees, created by Claude Code.
.claude/worktrees/

# the tray app (Rust build products and Tauri codegen)
src-tauri/target/
src-tauri/gen/

# the staged runtime a bundled .app carries — produced by `npm run tray:stage`
dist-runtime/
